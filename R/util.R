# temp_from_ptemp <- function(ptemp,sal,press)
#
#
# SA <- gsw::gsw_SA_from_SP(sal, p = press,
#                           longitude = l$longitude, latitude = l$latitude)
# res <- gsw::gsw_pt_from_t(SA = SA, t = l$temperature,
#                           p = l$pressure, p_ref = referencePressure)
